# Optional reverse proxy for automatic HTTPS (Let's Encrypt). Replace the domain, then run:
#   docker compose --profile https up -d
# Browser Widevine/EME (Phase 2 web player) requires a secure context, so HTTPS is needed there.
jtv.example.com {
    reverse_proxy jtv-server:8080
}
